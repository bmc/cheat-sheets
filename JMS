Sun's MQ JMS server:

- Start broker via:
	/opt/sun/mq/bin/imqbrokerd -javahome /usr/local/java/jdk
- Does not need to be run as root
- Add jars in /opt/sun/share/lib to CLASSPATH to run client programs.

TIBCO's JMS server:

- Set environment variable TIBEMS_ROOT to "/opt/tibco/ems"
- Run this command:
	. $TIBEMS_ROOT/samples/java/setup14.sh
- Start server via:
	/opt/tibco/ems/bin/tibjmsd
- Does not need to be run as root
